# Dev-only. Connect the /chat WebSocket directly to the Flask backend instead
# of through Vite's proxy (avoids `write EPIPE` proxy noise). Not used by
# `npm run build` — prod falls back to a same-origin ws:// URL.
VITE_WS_URL=ws://localhost:8000/chat
